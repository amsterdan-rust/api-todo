info:
  name: CREATE
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/todos"
  body:
    type: json
    data: |-
      {
        "title": 
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
